import { Screen } from '@/components/common/Screen';
import { Text } from 'react-native';

export default function SearchRoute() {
  return (
    <Screen>
      <Text>Suche — coming soon.</Text>
    </Screen>
  );
}
