import { Screen } from '@/components/common/Screen';
import { Text } from 'react-native';

export default function SettingsRoute() {
  return (
    <Screen>
      <Text>Einstellungen — coming soon.</Text>
    </Screen>
  );
}
